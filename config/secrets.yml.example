development:
  secret_key_base: "Use `rake secret` to generate new keys for the development and test sections."

test:
  secret_key_base: "Use `rake secret` to generate new keys for the development and test sections."

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>