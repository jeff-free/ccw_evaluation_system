<div class="page-header">
  <h1>請選擇活動</h1>
</div>

<div class="list-group">
  <% @events.each do |event| %>
    <%= link_to(event.name, "video_citizen_events_path(event)", class: "list-group-item") %>
  <% end %>
</div>