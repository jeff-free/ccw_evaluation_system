<%= simple_form_for([:admin, @committee]) do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, label: "委員會名稱" %>
  <%= f.input :description, label: "委員會介紹" %>

  <div class="form-actions">
    <%= f.button :submit, "送出" %>
  </div>
<% end %>
