<div class="page-header">
  <h1>請選擇立委</h1>
</div>

<div class="list-group">
  <% @courses.each do |course| %>
    <%= link_to(course.name, join_student_course_path(course), class: "list-group-item", method: :post) %>
  <% end %>
</div>